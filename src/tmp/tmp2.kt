package tmp

// - 輸出為小寫 a-f
// - extra leading 0 省略 （除了 input = 0, output = 0）
// - input: 32 bit signed int

fun toHex(num: Int): String {
    return ""
}

fun main() {
    println(toHex(26))
    println(toHex(-1))
    println(toHex(0))
    println(toHex(0))
}